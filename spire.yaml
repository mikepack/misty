runtime:
  language: typescript
  install: npm install
  test: echo "no tests configured"
  build: npm run build
  lint: npm run lint

agent:
  model: claude-sonnet-4-6
  stale: 10m
  timeout: 15m

branch:
  base: main
  pattern: "feat/{bead-id}"

pr:
  auto-merge: false
  # reviewers: []
  # labels: []

design:
  require_approval: true  # set false for automated pipelines

context:
  - CLAUDE.md
  - SPIRE.md
